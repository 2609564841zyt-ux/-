# 寻仙二级界面与电梯门动效

## Summary
给“寻仙”模块补一个独立二级界面，点击整张寻仙卡片后先播放“电梯门打开”转场，再进入一个参考 Moodie 的纵向滚动详情页。内容以图片为主，不加额外过程叙事文案，按 `10.gif -> 1-01.jpg -> 1-02.jpg -> 作品集商业化改版1_画板 1 副本 72.jpg` 的顺序组织。

## Key Changes
- 在品牌区把“寻仙”卡片从静态 `article` 改为可点击入口，链接到新的 `#xunxian-detail`。
- 新增一个寻仙详情层，结构沿用现有详情页体系：
  - 顶部保留 sticky HUD 和返回按钮
  - 首屏放 `10.gif` 做开场主视觉
  - 后续按 Moodie 的流程继续纵向堆叠 3 张图片
  - 不加入 narration 模块，保持 `Process_narration=false`
- 复用现有详情层的打开/关闭逻辑，让新页面能直接通过 hash 深链打开、返回品牌页、刷新后恢复状态。
- 为“电梯门打开”补一个更贴合寻仙的转场样式：
  - 仍沿用当前门形遮罩的触发机制
  - 视觉上改成更像电梯门的左右分离/合拢感
  - 仅在寻仙入口触发时强化这套动效，尽量不影响其他模块
- 把桌面上的 4 张素材复制进项目 `assets/`，并统一成稳定命名，方便部署和引用。

## Test Plan
- 点击寻仙卡片时，能看到电梯门转场，然后进入详情层。
- 详情层首屏正确显示 `10.gif`，后面 3 张图按预期顺序出现。
- 点击返回可回到品牌区，hash 状态正常更新。
- 刷新带 `#xunxian-detail` 的地址时，详情层应直接打开。
- 在移动端检查：图片不溢出、HUD 不遮挡内容、滚动顺畅。

## Assumptions
- 我按“整张寻仙卡片可点击”处理，不只限定窗口某个局部热区。
- `10.gif` 作为开场第一屏最合适，后面的三张图按你给的顺序接在后面。
- `Process_narration=false` 解释为“不要加叙事说明层”，只保留图片流和必要 HUD。
- 详情层尽量沿用现有 Moodie / 其他项目的技术结构，避免额外引入新的交互框架。
